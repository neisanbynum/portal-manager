{
  "compilerOptions": {
    "target": "ESNext",                        // Match latest JS (you're controlling the consumers)
    "module": "ESNext",                        // Modern ESM
    "moduleResolution": "Node",                // Standard for React/Webpack/Vite
    "outDir": "./dist",                        // Still output build here
    "rootDir": "./src",                        // Assume code is inside /src
    "jsx": "react-jsx",                        // Modern JSX (React 17+)
    "esModuleInterop": true,                   // Import default modules easily
    "allowSyntheticDefaultImports": true,      // Safer imports
    "skipLibCheck": true,                      // Speed up builds
    "forceConsistentCasingInFileNames": true,  // Prevent filesystem casing bugs
    "strict": true,                            // Always good for quality
    "verbatimModuleSyntax": true,               // Allow `export type *` (TS 5+)
    "declaration": true,                       // YES, emit types (future-proofing)
    "emitDeclarationOnly": false,              // Emit both JS + d.ts
    "declarationMap": false                    // ❗ OFF now (you don't need mapped .d.ts for local dev)
  },
  "include": ["src"],
  "exclude": ["node_modules", "dist"]
}
